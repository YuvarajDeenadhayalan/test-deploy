name: Multi-Env Deploy (Selective)

on:
  workflow_dispatch:
    inputs:
      target_env:
        description: "Select environment to deploy"
        required: true
        type: choice
        options:
          - dev
          - staging
          - production

jobs:
  deploy-dev:
    name: Deploy to Dev
    runs-on: ubuntu-latest
    environment: dev
    if: ${{ github.event.inputs.target_env == 'dev' }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Mock deploy to dev
        run: |
          echo "🚀 Deploying to DEV environment..."
          sleep 2
          echo "✅ Dev deployment complete!"

  deploy-staging:
    name: Deploy to Staging
    runs-on: ubuntu-latest
    environment: staging
    if: ${{ github.event.inputs.target_env == 'staging' }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Mock deploy to staging
        run: |
          echo "🚀 Deploying to STAGING environment..."
          sleep 2
          echo "✅ Staging deployment complete!"

  deploy-production:
    name: Deploy to Production
    runs-on: ubuntu-latest
    environment: production
    if: ${{ github.event.inputs.target_env == 'production' }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Mock deploy to production
        run: |
          echo "🚀 Deployecho "🚀 Deploying to PRODUCTION environment..."
          sleep 2
          echo "✅ Production deployment complete!"ing to PRODUCTION environment..."
          sleep 2
          echo "✅ Production deployment complete!"
